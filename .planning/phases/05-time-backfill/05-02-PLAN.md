---
phase: 05-time-backfill
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified: []
autonomous: false
requirements:
  - TIME-01

must_haves:
  truths:
    - "Dry-run output is reviewed and operator confirms the numbers look reasonable before any writes happen"
    - "After live run, meeting_time is non-null for at least 85 of the ~89 previously-null rows (parse failures are expected for a small number)"
    - "A second dry-run after the live run reports 0 WOULD FILL rows (idempotency confirmed)"
    - "No row that had a meeting_time before the run has a different meeting_time after"
  artifacts:
    - path: "public_html/api/scripts/backfill-times.php"
      provides: "Already created in plan 05-01"
  key_links:
    - from: "dry-run output"
      to: "operator approval"
      via: "checkpoint:human-verify"
      pattern: "checkpoint"
---

<objective>
Run backfill-times.php: dry-run first, operator review, then live run and idempotency check.

Purpose: Actually populate the ~89 missing meeting_time values — the script from plan 05-01 is inert until executed.
Output: Database rows updated, idempotency confirmed, execution logged.
</objective>

<execution_context>
@/home/patrickgartside/.claude/get-shit-done/workflows/execute-plan.md
@/home/patrickgartside/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/patrickgartside/dev/Access100/app website/.planning/phases/05-time-backfill/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dry-run — print what would be filled</name>
  <files></files>

  <read_first>
    - .planning/phases/05-time-backfill/05-01-SUMMARY.md — confirm script path
  </read_first>

  <action>
Run the script in dry-run mode and capture full output for operator review:

  docker exec appwebsite-app-1 php /var/www/html/api/scripts/backfill-times.php --dry-run

Print the complete stdout so the operator can read it before approving the live run.
  </action>

  <acceptance_criteria>
    - Command exits 0: $? equals 0
    - Output contains "--- Summary ---"
    - Output contains "DRY RUN"
    - At least one "WOULD FILL" line appears (or output explains zero matches with a plausible reason)
  </acceptance_criteria>

  <verify>
    <automated>docker exec appwebsite-app-1 php /var/www/html/api/scripts/backfill-times.php --dry-run</automated>
  </verify>

  <done>Dry-run completes with exit 0, summary block visible, WOULD FILL lines present for the expected sources (NCO ~41, HNL Boards ~11, eHawaii ~37).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Dry-run output from backfill-times.php showing every meeting that would receive a time value, per source, with the expected counts (~41 NCO, ~11 Honolulu Boards, ~37 eHawaii).</what-built>
  <how-to-verify>
    1. Read the dry-run output printed above.
    2. Confirm totals are in the expected ballpark: NCO ~41, Honolulu Boards ~11, eHawaii ~37.
    3. Spot-check 2-3 WOULD FILL lines — do the time values (e.g. "07:00:00", "19:30:00") look like real meeting times?
    4. Confirm no source appears under "Total skipped" with an unexpectedly high count (that would mean times were already present and a prior run may have partially executed).
    5. If anything looks wrong (zero fills, wildly off counts, obviously bogus times like "00:00:00"), type "reject" and describe the issue.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the live run, or describe any issues to abort.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Live run + idempotency check</name>
  <files></files>

  <read_first>
    - Nothing additional — proceed only after checkpoint approval
  </read_first>

  <action>
Step 1 — Live run:
  docker exec appwebsite-app-1 php /var/www/html/api/scripts/backfill-times.php

Print full output. Confirm exit code 0 and that "Total filled:" in the summary matches the "Total filled:" count from the dry-run (they must be equal).

Step 2 — Idempotency check (second dry-run):
  docker exec appwebsite-app-1 php /var/www/html/api/scripts/backfill-times.php --dry-run

Print full output. Confirm that "Total filled:" in the summary is 0 (all previously-null rows now have a time; no rows remain to fill). If it is not 0, print the WOULD FILL lines and note them as exceptions (e.g. rows with unparseable raw_rss_data — acceptable).

Step 3 — DB spot-check (confirm no pre-existing times were overwritten):
  docker exec appwebsite-app-1 php -r "
    require '/var/www/html/api/config.php';
    \$pdo = get_db();
    \$r = \$pdo->query(\"SELECT source, COUNT(*) as n, COUNT(meeting_time) as has_time FROM meetings WHERE source IN ('nco','honolulu_boards','ehawaii') GROUP BY source\")->fetchAll();
    foreach (\$r as \$row) echo \$row['source'] . ': ' . \$row['has_time'] . '/' . \$row['n'] . ' have meeting_time\n';
  "

Print counts. Expected: NCO ≥ 3/44, HNL Boards ≥ 10/21, eHawaii ≥ 313/350 (pre-existing times were already there; newly filled rows bring totals up).
  </action>

  <acceptance_criteria>
    - Live run exits 0
    - Live run "Total filled" matches dry-run "Total filled" count
    - Second dry-run "Total filled" = 0
    - DB spot-check shows meeting_time counts increased for NCO and/or HNL Boards and/or eHawaii compared to the ~89 previously-null rows
  </acceptance_criteria>

  <verify>
    <automated>docker exec appwebsite-app-1 php /var/www/html/api/scripts/backfill-times.php --dry-run 2>&1 | grep 'Total filled'</automated>
  </verify>

  <done>Live run fills the expected rows, second dry-run reports 0 fills, DB spot-check shows updated counts. Phase 5 complete.</done>
</task>

</tasks>

<verification>
Second dry-run output shows "Total filled: 0 (DRY RUN — no writes)".
DB spot-check shows non-zero meeting_time counts for all three sources.
</verification>

<success_criteria>
All ~89 previously-null meeting_time rows populated. Idempotency confirmed (second dry-run = 0 fills). No pre-existing time values changed.
</success_criteria>

<output>
After completion, create .planning/phases/05-time-backfill/05-02-SUMMARY.md
</output>
